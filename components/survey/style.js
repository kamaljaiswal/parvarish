import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  surveyRoot: {
    width: "100%",
    padding: 10,
  },
  ques: {
    paddingBottom: 18,
  },
  questionText: {
    fontSize: 18,
    color: "#000",
    marginBottom: 15,
    lineHeight: 25,
  },
  bold: {
    fontWeight: "bold",
  },
});
export default styles;
