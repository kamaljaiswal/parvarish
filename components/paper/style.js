import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  surfaceRoot: {
    padding: 15,
    elevation: 4,
    borderWidth: 3,
    borderColor: "#003152",
  },
});
export default styles;
